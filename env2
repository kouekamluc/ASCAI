# Django Environment Configuration (Render-ready)
#
# Copy this file to .env (or paste into Render's Environment tab) and replace
# placeholder values with your real credentials. All variables mirror
# config/settings.py so no additional changes are needed.

# --- Core Django ---
SECRET_KEY=v5acly$1r@lzqz6kpg%91&dp$kw&ulk^&1=_v&ujf-u)&8dv8u
DEBUG=False
DJANGO_ENVIRONMENT=production
ALLOWED_HOSTS=your-domain.com,www.your-domain.com

# --- Database (use whichever block fits your setup) ---
# Option A: Render Postgres connection string
DATABASE_URL=postgres://user:password@dpg-yourdb.render.com:5432/ascai?sslmode=require

# Option B: Individual fields (leave DATABASE_URL empty when using these)
DB_NAME=ASCAI
DB_USER=ascai_user
DB_PASSWORD=replace-with-db-password
DB_HOST=dpg-yourdb.render.com
DB_PORT=5432
DB_SSLMODE=require

# --- Email ---
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=app-specific-password
DEFAULT_FROM_EMAIL=noreply@your-domain.com

# --- AWS S3 (optional) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=eu-central-1

# --- Stripe / PayPal (optional) ---
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_MODE=live

# --- Redis / Celery ---
REDIS_URL=redis://default:password@redis-host.render.com:6379/0
CELERY_BROKER_URL=${REDIS_URL}
CELERY_RESULT_BACKEND=${REDIS_URL}

# --- CORS / Site ---
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://admin.your-domain.com
SITE_URL=https://your-domain.com

# --- Logging ---
DJANGO_LOG_LEVEL=INFO

# --- Google OAuth ---
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# --- Testing DB (optional override) ---
TEST_DB_NAME=test_ASCAI

